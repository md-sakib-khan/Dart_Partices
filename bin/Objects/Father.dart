abstract class Father {
  // this is a abstract class, so cant create this object from anywhere, but can be accessed from child class.
  void addTow() {
    print("Sum From Father = ${2 + 3} ");
  }

  void Multiply() {
    print("Sum From Father = ${2 * 3} ");
  }
}
